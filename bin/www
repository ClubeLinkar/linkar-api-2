#!/usr/bin/env node
var app = require('../src/app');

app.set('port', process.env.PORT || 8080);

var server = app.listen(app.get('port'), function() {
  console.log('Linkar API executando na porta ' + server.address().port);
});
